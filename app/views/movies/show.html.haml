= image_tag(Imdb::Movie.new(@movie.imdb_id).poster)

%p
  %b Title:
  = @movie.title
%p
  %b Imdb:
  = @movie.imdb_id
%p
  %b Year:
  = @movie.year

= link_to 'Edit', edit_movie_path(@movie)
\|
= link_to 'Back', movies_path
